server:
  port: 8082

spring:
  application:
    name: studylog-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres-service.default.svc.cluster.local}:${DB_PORT:5432}/mydb
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
